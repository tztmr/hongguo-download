# v0.5.7

- 修复 macOS 使用系统代理时，视频管理 AI 参考原封面生成失败的问题。YouTube 原封面下载现在使用与其他 YouTube 请求一致的系统网络配置。
- 原封面地址仅允许 YouTube 图片 CDN 的 HTTPS 默认端口，禁止携带账号及跳转；校验实际图片格式，并对响应长度和流式读取设置 8 MB 上限。
- 原封面网络失败时给出明确提示，已生成的标题和说明继续保留；重试仅处理未完成部分。

验证覆盖 CDN 地址校验、重定向拒绝、无效图片、声明大小与流式大小限制。已在系统代理下成功读取实测视频的原封面；AI 生成与 YouTube 同步结果以本次实测记录及 Release 说明为准。
